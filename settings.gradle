include ':ar'
include ':pc'
include ':construct'
include ':utils'
if (!System.getenv("CI")) {
    // Native Project
    include ':chisel'
    if(System.getenv("PCL")){
    	include ':construct-native'
    }
}
